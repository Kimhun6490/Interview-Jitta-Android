query StockQuery($id: String!, $stockId: Int!) {
    stock(id: $id, stockId: $stockId) {
        title
        id
        stockId
        isFollowing
        sector {
            id
            name
        }
        industry
        company {
            link {
                url
            }
        }
    }
}