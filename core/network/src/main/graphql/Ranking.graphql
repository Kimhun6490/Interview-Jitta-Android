query RankingQuery($market: String!, $first: Int @optional, $offset: Int @optional) {
    jittaRanking(filter:{ market: $market }) {
        count
        data {
            id
            name
            sector {
                id
                name
            }
            rank
        }
    }
}