query RankingQuery($market: String!, $page: Int @optional, $limit: Int @optional) {
    jittaRanking(filter:{ market: $market, page: $page, limit: $limit }) {
        count
        data {
            id
            stockId
            name
            sector {
                id
                name
            }
            rank
        }
    }
}